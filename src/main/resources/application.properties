spring.application.name=glowbook
server.port=${PORT:8080}
glowbook.business-time-zone=${GLOWBOOK_BUSINESS_TIME_ZONE:Europe/Istanbul}

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=${DB_URL:jdbc:mysql://${MYSQLHOST:localhost}:${MYSQLPORT:3306}/${MYSQLDATABASE:glowbook}?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC&useUnicode=true&characterEncoding=UTF-8}
spring.datasource.username=${DB_USERNAME:${MYSQLUSER:root}}
spring.datasource.password=${DB_PASSWORD:${MYSQLPASSWORD:}}
spring.datasource.hikari.connection-timeout=${DB_CONNECTION_TIMEOUT_MS:30000}
spring.datasource.hikari.initialization-fail-timeout=${DB_INITIALIZATION_FAIL_TIMEOUT_MS:60000}
spring.datasource.hikari.maximum-pool-size=${DB_MAX_POOL_SIZE:5}
spring.datasource.hikari.minimum-idle=${DB_MIN_IDLE:1}
spring.datasource.hikari.idle-timeout=${DB_IDLE_TIMEOUT_MS:300000}
spring.datasource.hikari.max-lifetime=${DB_MAX_LIFETIME_MS:1200000}
spring.jpa.hibernate.ddl-auto=${HIBERNATE_DDL_AUTO:update}
spring.jpa.open-in-view=false

app.jwt.secret=${JWT_SECRET:glowbook-local-secret-change-me}
app.jwt.expiration-seconds=86400
app.jwt.refresh-expiration-days=30
app.scheduler.reminder-cron=0 0 10 * * *
app.cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:3000,http://localhost:5173,http://localhost:8080,https://*.vercel.app}
app.sms.provider=${SMS_PROVIDER:log}
app.demo-users.enabled=${ENABLE_DEMO_USERS:false}
app.demo-users.admin-password=${DEMO_ADMIN_PASSWORD:}
app.demo-users.employee-password=${DEMO_EMPLOYEE_PASSWORD:}
app.demo-users.customer-password=${DEMO_CUSTOMER_PASSWORD:}
twilio.account-sid=${TWILIO_ACCOUNT_SID:}
twilio.auth-token=${TWILIO_AUTH_TOKEN:}
twilio.from-number=${TWILIO_FROM_NUMBER:}

# Password reset - see docs. provider=log (default) sends nothing, only logs;
# set MAIL_PROVIDER=smtp plus the connection variables below once a real
# mailbox is configured.
app.frontend-url=${FRONTEND_URL:https://glowbook-flutter.vercel.app}
app.mail.provider=${MAIL_PROVIDER:log}
app.mail.from=${MAIL_FROM:}
spring.mail.host=${MAIL_HOST:}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:}
spring.mail.password=${MAIL_PASSWORD:}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# WhatsApp Business Platform / Cloud API - see docs/WHATSAPP_NOTIFICATIONS.md.
# enabled=false (default) is the product switch: no salon uses GlowBook live yet, so
# nothing is sent, not even through the no-op logging provider. provider=log (default)
# is the technical switch for once enabled=true: it makes zero external requests; set
# to "meta" only once WHATSAPP_ACCESS_TOKEN/WHATSAPP_PHONE_NUMBER_ID/the approved
# template are configured.
app.whatsapp.enabled=${WHATSAPP_ENABLED:false}
app.whatsapp.provider=${WHATSAPP_PROVIDER:log}
whatsapp.access-token=${WHATSAPP_ACCESS_TOKEN:}
whatsapp.phone-number-id=${WHATSAPP_PHONE_NUMBER_ID:}
whatsapp.business-account-id=${WHATSAPP_BUSINESS_ACCOUNT_ID:}
whatsapp.graph-api-version=${WHATSAPP_GRAPH_API_VERSION:v21.0}
whatsapp.template-name=${WHATSAPP_TEMPLATE_NAME:appointment_confirmation_tr}
whatsapp.template-language=${WHATSAPP_TEMPLATE_LANGUAGE:tr}
whatsapp.webhook-verify-token=${WHATSAPP_WEBHOOK_VERIFY_TOKEN:}
whatsapp.app-secret=${WHATSAPP_APP_SECRET:}
